{
  "name": "ninja-rope-run",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "homepage": "https://yashkh-dev.github.io/ninja-rope-run/",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "typescript": "~5.9.3",
    "vite": "^7.2.4"
  },
  "dependencies": {
    "phaser": "^3.90.0"
  }
}